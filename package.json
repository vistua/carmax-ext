{
  "name": "carmax-auctions-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension — CarMax Auctions vehicle scraper",
  "scripts": {
    "build": "node scripts/build.js",
    "zip": "node scripts/build.js --zip",
    "test": "playwright test",
    "test:ci": "playwright test --reporter=github",
    "lint": "eslint *.js error-handler.js",
    "publish:store": "node scripts/publish.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "archiver": "^7.0.0",
    "eslint": "^8.57.0"
  }
}
